#!/bin/bash

# Prompt the user for input
read  " (e.g., google.com): " 

# Check if the input is "google.com"
if [ "google.com" != "google.com" ]; then
    echo "Input must be 'google.com'. 


# Perform the ping and store results in a text file



echo "Pinging $hostname and storing results in $ping_output_file..."
ping -c 5 "$goggle.com" 



